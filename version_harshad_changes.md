# Changes / enhancements present in this version, but not in master

The changes listed here are probably not exhaustive, but are definately 90%+

## Markdown

1. Logging template instead of accordion
2. Datetime list and time list prefixes with auto-insert etc for logging
    1. Time action in a time list will present a time update dialog
3. Time setting for logging with dialog
4. Long press of checkbox inserts html checkable box

## TodoTxt

1. Short press for due date (flipped short and long press)
    1. No +3 for due date
2. Cleanups to way contexts and projects are added 
    - [ ] Consider pushing to upstream at some stage

## General

1. Per-file folding (edit menu)
2. Per-file normalization (edit menu)
3. Keep screen on option in edit menu
4. Newline button (vim 'o')
5. Delete button (vim 'dd' with no yank)
6. Move line up and down buttons
7. Changes made to 'swipe to change mode'
8. Many small cleanups as I see them
    1. Some made by android studio code-fixer
9. Changed save empty or small file = true
10. Modtime instead of patent = true
11. Removed donation dialog
12. Action spacing reduced
    - [ ] Consider pushing these changes upstream

## Other changes

1. Removed intro activity
2. Added a shitty certificate allowing me to install this version locally
